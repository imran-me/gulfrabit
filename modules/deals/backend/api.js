/**
 * Deals · module API (mock)
 * Derives deals from the shared catalog today. Replace with GET /deals later.
 */
export { getDeals } from '../../catalog/backend/api.js';
// TODO: backend — server owns discount rules + campaign windows.
