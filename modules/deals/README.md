# Module · Deals

`deals.html` — Deals & Offers: all discounted products (originalPrice > price),
biggest-discount highlights, and a sortable grid.

- Fragment `_fragments/deals.main.html`, styles `deals.css`, behaviour
  `deals-page.js` (uses `data-service.getDeals()`).

## Backend
`backend/endpoints.md` + `backend/api.js` — a promotions/deals feed (server
computes active discounts / campaign windows).
